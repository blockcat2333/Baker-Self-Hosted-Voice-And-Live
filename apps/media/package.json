{
  "name": "@baker/media",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src",
    "start": "node dist/index.js",
    "test": "vitest run apps/media",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@baker/protocol": "workspace:*",
    "@baker/shared": "workspace:*",
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.8.5"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "tsx": "^4.19.2"
  }
}
