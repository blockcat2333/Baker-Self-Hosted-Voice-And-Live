{
  "name": "@baker/sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "lint": "eslint src",
    "test": "vitest run packages/sdk",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@baker/protocol": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5"
  }
}
